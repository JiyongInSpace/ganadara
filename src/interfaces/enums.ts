
// 단원의 난이도
export enum LEVELS {
    BEG = 'beginner',
    LOWINT = 'lower_intermediate',
    INT = 'intermediate',
    ADV = 'advanced'
}

// 단원의 상태
export enum CHAPTER_STATES {
    NOT_STARTED = 'not_started',
    COMPLETED = 'completed',
    TESTED = 'tested'
}